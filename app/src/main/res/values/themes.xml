<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base theme for the Activity window; Compose (QaWorkspaceTheme) drives actual UI colors.
         DayNight qua resource qualifier (values-night) — framework không có parent DayNight,
         và ta không kéo Material Components XML (Compose-only). -->
    <style name="Theme.QaWorkspace" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@android:color/white</item>
    </style>
</resources>
